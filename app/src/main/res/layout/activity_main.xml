<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="What would you like to do?"
        android:id="@+id/textView"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Restaurant List"
        android:id="@+id/btnRestaurantList"
        android:layout_below="@+id/textView"
        android:layout_marginTop="31dp"
        android:onClick="onClick"
        android:layout_alignRight="@+id/btnAddRestaurant"
        android:layout_alignEnd="@+id/btnAddRestaurant"
        android:layout_alignLeft="@+id/btnAddRestaurant"
        android:layout_alignStart="@+id/btnAddRestaurant" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Add New Restaurant"
        android:id="@+id/btnAddRestaurant"
        android:layout_below="@+id/btnRestaurantList"
        android:layout_centerHorizontal="true"
        android:onClick="onClick" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="About App"
        android:id="@+id/btnAbout"
        android:layout_below="@+id/btnAddRestaurant"
        android:layout_alignRight="@+id/btnAddRestaurant"
        android:layout_alignEnd="@+id/btnAddRestaurant"
        android:layout_alignLeft="@+id/btnAddRestaurant"
        android:layout_alignStart="@+id/btnAddRestaurant"
        android:onClick="onClick" />
</RelativeLayout>
